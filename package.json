{
  "name": "swiftdialog-docs",
  "type": "module",
  "version": "0.0.1",
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "pnpm run build && wrangler pages deploy dist",
    "deploy:preview": "pnpm run build && wrangler pages deploy dist --branch=preview",
    "cf:dev": "npx wrangler pages dev dist"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.35.3",
    "astro": "^5.0.0",
    "sharp": "^0.33.0",
    "wrangler": "^4.40.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}